{
  "name": "rabbitmq-consumer",
  "version": "0.0.1",
  "description": "RabbitMQ + Node.js using Docker Compose",
  "main": "consumer.js",
  "scripts": {
    "build": "tsc -p ./tsconfig.json && for f in *.js; do mv -- \"$f\" \"${f%.js}.mjs\"; done"
  },
  "keywords": [],
  "author": "Marco Lehmann <m99@posteo.de>",
  "license": "MIT",
  "dependencies": {
    "amqplib": "^0.8.0"
  },
  "devDependencies": {
    "@types/amqplib": "^0.8.2",
    "ts-node": "^10.5.0",
    "typescript": "^4.5.5"
  }
}
